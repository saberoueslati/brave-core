This folder contains the `@mozilla/readability` dependency 2 changes:

- The `export` keyword is added to the `Readability` and `isProbablyReaderable` methods so that it can be used in TypeScript imports.
- The `module.exports` are removed as they're not needed.

This dependency is considered frozen and will not be further modified.
